import { css, styled } from '../../style/stitches.config';

export const Wrapper = styled('div', {
  width: '32px',
  height: '32px',
  borderRadius: '50%',
  position: 'relative',
  overflow: 'hidden',
});
